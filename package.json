{
  "name": "ring-of-fire",
  "version": "0.0.13",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bash -c \"./build.sh\"",
    "serve": "firebase serve --only hosting -p 5000",
    "emu": "npm run build && firebase emulators:start --only functions,hosting",
    "deploy": "npm run build && firebase deploy --only hosting",
    "start-working": "open-browsers.bat && npm run serve"
  },
  "author": "",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bytemunch/ring-of-fire.git"
  },
  "bugs": {
    "url": "https://github.com/bytemunch/ring-of-fire/issues"
  },
  "homepage": "https://github.com/bytemunch/ring-of-fire#readme",
  "dependencies": {
    "firebase": "^8.8.1",
    "firebase-admin": "^9.11.0",
    "firebase-functions": "^3.14.1",
    "firebase-tools": "^9.16.0"
  },
  "devDependencies": {}
}
